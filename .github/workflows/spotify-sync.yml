name: Sync Spotify data to /now

on:
  schedule:
    - cron: '5 */3 * * *'  # every 3h at :05 UTC. Was every 30 min — 1,440 billed min/month on the shared
    # Actions budget (plus a Pages rebuild per commit). Spotify's recently-played API returns the last 50
    # tracks, which 3h still covers for normal listening. Cron must be UTC.
  workflow_dispatch:

concurrency:
  group: now-html-writer
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  update-spotify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Sync to latest main (avoid stale checkout when queued behind another run)
        run: |
          git fetch origin main
          git reset --hard origin/main

      - name: Fetch Spotify data and update /now/
        env:
          SPOTIFY_CLIENT_ID:     ${{ secrets.SPOTIFY_CLIENT_ID }}
          SPOTIFY_CLIENT_SECRET: ${{ secrets.SPOTIFY_CLIENT_SECRET }}
          SPOTIFY_REFRESH_TOKEN: ${{ secrets.SPOTIFY_REFRESH_TOKEN }}
        run: python3 bin/update-spotify.py

      - name: Commit and push if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add now/index.html .feeds-heartbeat.json
          [ -f .spotify-state.json ] && git add .spotify-state.json || true
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore: update Spotify data on /now page"
            git pull --rebase origin main
            git push
          fi
